🧪 Add test file for Music Pattern Resource

🎯 What: Added `test/cantor/music/pattern_test.exs` to fill the testing gap for the `Cantor.Music.Pattern` Ash resource.
📊 Coverage: Added comprehensive tests for the `:create` action, `:update` action, and all read actions (`:public`, `:by_type`, `:by_tag`, `:by_difficulty`). The tests cover happy paths, default value initializations, and constraints (e.g. required attributes, enum types, numerical boundaries).
✨ Result: The test coverage is significantly improved by verifying that pattern creations and read queries enforce the proper conditions and return accurate patterns.
