## 2026-04-03 - Add ARIA Labels to Cadence Notebook\n**Learning:** Icon-only buttons and unassociated textareas are common accessibility issues in interactive notebooks. Adding `aria-label` attributes and `title` tooltips to these elements significantly improves screen reader navigation and general usability.\n**Action:** Proactively check for `aria-label` on icon-only buttons and ensure all form inputs, especially complex ones like code editors, have explicit accessibility associations.
